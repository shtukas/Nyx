
NxTag.sqlite3

CREATE TABLE _tags_ (_uuid_ text, _string_ text);

NxTag {
    "uuid"        : String
    "entityType"  : "NxTag"
    "description" : String
}

There are no arrows between tags and other entities. Only links.
